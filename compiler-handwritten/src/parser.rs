// pub(crate) struct Parser {}
/*

if let Some(...) = scan() {
    if mem::discriminant(...) == expected {
        self.advance();
    }
} else {
    report? Diagnostic
        .message(unexpected early EOF)
        .label(primary(previous range))
}

*/
